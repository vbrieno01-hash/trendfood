

## Plano: Tema Padrão Branco (Light)

### Alteração
Mudar o `defaultTheme` de `"system"` para `"light"` no `ThemeProvider` em `src/App.tsx`. Assim todos os usuários começam no modo claro (branco), e só quem clicar no toggle de tema muda para dark.

### Arquivo alterado
- `src/App.tsx` — linha do `ThemeProvider`: trocar `defaultTheme="system"` para `defaultTheme="light"`

